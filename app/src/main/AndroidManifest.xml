<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.huabao.ttsdkdemo">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.SearchTicTagDeviceActivity"
            android:label="@string/btn_search_device"/>
        <activity
            android:name=".activity.TicTagDetailActivity"
            android:label="@string/btn_device_detail"/>

        <!--蒲公英AppKey-->
        <meta-data
            android:name="PGYER_APPID"
            android:value="740a839d3cee99b7434064cea4914bb8">
        </meta-data>
    </application>

</manifest>